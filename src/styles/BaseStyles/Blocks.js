import { createGlobalStyle } from 'styled-components';

export const Blocks = createGlobalStyle`
  html {
    height: 100%;
  }

  body {
    min-width: 320px;
    height: 100%;
  }

  .root {
    height: 100%;
  }
`;
